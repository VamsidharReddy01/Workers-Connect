# WorkersBridge Mobile

React Native app scaffolded with Expo for the existing Django REST backend.

## Run

```bash
npm install
npm start
```

Set `EXPO_PUBLIC_API_BASE_URL` in `.env` if Django is not reachable at the default Android emulator URL `http://10.0.2.2:8000`.
